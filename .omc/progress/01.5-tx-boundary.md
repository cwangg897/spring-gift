# PR #6 (01.5-tx-boundary 횡단) 진행 추적

## 목표 (refactor-progress/01.5-tx-boundary.md)
임시 `OrderFacade` 에 `@Transactional` 부착으로 `OrderController.createOrder` 의 무트랜잭션 6단계를 원자화. 비즈니스 로직 이동은 06-order Phase A 에서 정식 service 승격 시.

## 단계별 진행

- [ ] 1. `gift.order.OrderFacade` 신설 — `@Service @Transactional @Deprecated(forRemoval=true, since="01.5")`, `createOrder(member, OrderRequest)`. 컨트롤러의 6단계 로직 그대로 이전.
- [ ] 2. `OrderController` 가 `orderFacade.createOrder` 위임만 호출. `OptionRepository`, `WishRepository`, `MemberRepository`, `KakaoMessageClient` 직접 의존 제거 (auth 만 유지).
- [ ] 3. 카카오 알림은 facade 안 그대로 호출 (이벤트화는 06-B). `catch (Exception ignored)` → WARN 로깅.
- [x] 4. `OrderFacadeIntegrationTest` 3건 (성공 / 포인트 부족 시 롤백 / 옵션 미존재)
- [x] 5. `./gradlew test` 19/0/0
- [x] 6. doc 갱신 + Architect APPROVE + 커밋 분리 (`27eaab9 feat(order)` + `(docs)`) + cancel 진행

## 횡단 단계 원칙
- **facade only**: 비즈니스 로직 이동 없음. 컨트롤러의 6단계 흐름을 그대로 facade 로 옮긴다.
- 응답 코드, 응답 본문, 동작 순서 변경 0
- `@Deprecated(forRemoval=true)` 박제로 06-order Phase A 에서 폐기 차단 메커니즘 활성화

## 비범위 (06-order)
- facade → `OrderService` 승격 (06-A)
- 위시 정리 누락 동작 구현 (06-B.1)
- 카카오 알림 `@TransactionalEventListener(AFTER_COMMIT)` 이벤트화 (06-B.2)
