# 01.5-tx-boundary · 트랜잭션 경계 임시 부착 (횡단)

> **PR #6**. 횡단 단계, 단일 phase.
> **목표**: prd 라인 9 "트랜잭션 경계 세우기" 를 끝까지 미루지 않고 첫 6번째 PR 안에 해결. 임시 `OrderFacade` 에 `@Transactional` 만 부착 — 비즈니스 로직 이동은 06-order 에서 정식 service 승격.

---

## 1. 현재 상태 진단

- 패키지: `gift.order`
- 대상 파일:
  - `OrderController.java:69-102` — `createOrder` 6단계 (auth → option validate → subtract stock → deduct point → save order → kakao notify) **무트랜잭션**
  - `OrderController.java:88,93,96` — repository 직접 save (부분 실패 시 데이터 불일치)
  - `OrderController.java:108-112` — `sendKakaoMessageIfPossible`: `catch (Exception ignored)` 침묵

---

## 2. 산출물 + 체크리스트 (단일 phase, facade only)

- [x] 6.1 `gift.order.OrderFacade` 신설 — `@Service @Transactional @Deprecated(forRemoval=true, since="01.5")`, 6단계 로직 그대로 이전 ✓
- [x] 6.2 `OrderController` 위임 — 6 의존 → 3 (`OrderRepository`, `AuthenticationResolver`, `OrderFacade`) ✓
- [x] 6.3 `@Deprecated(forRemoval=true, since="01.5")` + javadoc 박제 ✓
- [x] 6.4 `catch (Exception ignored)` → `log.warn(...)` (orderId/memberId 포함) ✓
- [x] 6.5 `OrderFacadeIntegrationTest` 3건: happy path / 포인트 부족 시 롤백 / 옵션 미존재 시 NotFoundException ✓
- [x] 부수: `gift.order.OrderOptionNotFoundException extends NotFoundException`(404) 신설 — 옵션 누락 응답을 명시적 404 로

---

## 3. 검증 명령

```
./gradlew test
grep -rn "OrderFacade" src/   # 06-order 완료 시점에 0건이 목표
```

---

## 4. 변경 로그

- 2026-05-16: 횡단 단계 완료 — 임시 `OrderFacade` 도입 + `@Transactional`, `OrderController` 위임. `OrderFacadeIntegrationTest` 3건 (성공 / 롤백 / 옵션 미존재). `./gradlew test` 19/0/0. **`OrderFacade` 는 06-order Phase A 에서 폐기 예정** (`@Deprecated(forRemoval=true, since="01.5")` 박제).
